"use client"

/**
 * Stub for the create-manifest modal. Task 10 of the manifest-module plan
 * fills in the real implementation (form fields + useAction + redirect).
 * Existing today only so the manifest-list import resolves.
 */
interface Props {
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function CreateManifestModal(_props: Props) {
  return null
}
