"use client"

interface Props {
  description: string
  setDescription: (v: string) => void
  tags: string[]
  setTags: (t: string[]) => void
}

export function SettingsPanel(_props: Props) {
  return null
}
